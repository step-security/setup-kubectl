{
   "name": "setup-kubectl-action",
   "version": "5.0.0",
   "private": true,
   "main": "dist/index.js",
   "type": "module",
   "scripts": {
      "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\"",
      "typecheck": "tsc --noEmit",
      "test": "vitest run",
      "test-coverage": "vitest run --coverage",
      "format": "prettier --write .",
      "format-check": "prettier --check ."
   },
   "keywords": [
      "actions",
      "node",
      "setup"
   ],
   "author": "step-security",
   "license": "MIT",
   "dependencies": {
      "@actions/core": "^3.0.0",
      "@actions/exec": "^3.0.0",
      "@actions/tool-cache": "^4.0.0",
      "axios": "^1.10.0"
   },
   "devDependencies": {
      "@types/node": "^24.0.2",
      "esbuild": "^0.27.4",
      "prettier": "3.5.3",
      "typescript": "5.8.3",
      "vitest": "^4.0.18"
   }
}
